# 學習與對話約定（以本檔為準）

> **給 AI／Cursor Agent：** 每次回覆使用者前，請先閱讀本檔與 `.cursor/rules/user-learning-agreements.mdc`，並遵守下列規則。  
> **給使用者：** 本檔放在 GitHub 上，可隨時打開核對；有新約定就改這份檔，不以聊天「有沒有印象」為準。

最後更新：2026-08-05（機敏遮罩＝元素同構＋同位元組長度；解鎖規則待定）

> **複製給其他 Agent：** 使用 [`給其他Agent的學習法簡報.md`](./給其他Agent的學習法簡報.md)（內含「最短可貼版」）。本檔保留完整規則與課程進度。

---

## 0. 現行主線（以本節為準）

| 主線 | 狀態 | 說明 |
|------|------|------|
| A. 公司簽核系統（本倉庫 Approval） | ▶ **現行** | 紙本風網站＋可設定欄位＋簽名流水線＋類 LINE 對話＋共用／個人雙層資料；取代加價 Power Apps／Automate |
| B. 動手學大模型（dive-into-llms） | ⏸ 暫停 | 總進度 67%；下次從 G4 Ch1 檔案導讀繼續 |

使用者說「都可以／沒想法」時：走 **主線 A**，路徑由 Agent 選定（見 §4A）。

---

## 1. 語言

- 一律使用**台灣繁體中文**回答。
- 使用者語音轉成文字時，即使出現簡體、錯字或中英混雜，仍以語意理解，並用**繁體中文**回覆。
- 「方體中文」視為「繁體中文」。
- 避免中國大陸用語與播報腔（例：不要用「軟件／視頻／什麽」等；改用「軟體／影片／什麼」等台灣常用詞）。

---

## 2. 教學節奏（GitHub／NotebookLM／陌生專案）

- 一般情況：**一次只給一步**；等使用者回報完成後，再給下一步。
- **限時衝刺／演示模式**（例如 3 小時）：改為「時間塊＋檢查點」，每塊開始時一次講清目標與產出，塊內可連續推進；仍避免一次丟整門課。
- 先用地圖與優先順序說明，再深讀細節。
- **結構偏好**：使用者偏好先全盤架構，再填細節與實作；限時學習時優先「全庫地圖」，避免過早鎖死單一章實作。
- 組合方式：
  - **Cursor／本 Agent**：結構、路徑、問卷、跑 code、排錯。
  - **NotebookLM**：依使用者上傳來源深讀、摘要、自測、語音摘要。
  - **Gemini 等一般對話**：廣問概念可以；若要與指定教材一致，以 NotebookLM 來源為準。

---

## 3. NotebookLM 語音摘要提示詞（預設）

產生語音摘要時，提示詞預設使用（可整段貼上）：

```
請用台灣繁體中文，用語與語氣要像台灣日常口語，不要用中國大陸用語、用詞或播報腔。

像媽媽在說故事那樣：溫柔、清楚、好懂，有一點生活比喻，不要像教科書或新聞主播。

對象是完全初學的人。請依序說清楚本章重點流程。
少講參數細節，多講每一步在做什麼、為什麼要做。
最後用一句很白話的話，把整章串起來。
```

設定建議：長度「中」、語言「繁體中文」、來源為當前章節檔案。

---

## 4A. 目前學習進度（公司簽核系統／本倉庫 Approval）— **現行主線**

- 專案：`https://github.com/hyi1105/Approval`
- 背景：公司不想再為 Power Apps／Power Automate 進階功能加價；要做**紙本風純文字網站**＋可設定欄位的簽核表單＋簽名流水線＋類 LINE 對話格＋依身分顯示欄位。
- **Agent 已選定路徑（依 2026-08-04 產品描述更新）：**
  1. **介面**＝紙本風純文字網頁（可設計預覽）；不是先做 Teams 當唯一畫面
  2. **表單引擎**＝JSON schema（欄位型態／必填／條件／顯示／唯讀）；範例見 `schema/form-schema.example.json`
  3. **共用資料**＝SharePoint 清單（申請單公開欄、狀態、簽核線）
  4. **個人隱私欄**＝個人儲存空間（OneDrive／個人清單）；應用程式只存 user ID，畫面依 ID 讀出後填入「僅自己可見」欄；別人那邊**根本沒有這格內容**
  5. **通知**＝Teams（可選）：有申請／待簽時跳出；對話主體在網站「對話室」
  6. **身分**＝登入 ID（公司環境優先 Entra ID；手機可對應同一人）
- **本階段不做：** 付費 Power Platform 進階連接器、一開始就接內網 SQL。

### 4A.0 產品願景（紙本簽核＋對話室）

使用者要的不是「再做一套 Power App」，而是：

| 區塊 | 行為 |
|------|------|
| 紙本表單 | 純文字、像紙；欄位數可設（例如 5～6）；每欄有**內部名**與**顯示名** |
| 欄位型態 | 文字／數字／下拉／是非／人員（人對人） |
| 欄位規則 | 必填／條件才填／是否顯示／設計畫面預覽／唯讀或可編 |
| 按鈕 | 送出、核准、駁回（另可退回、通知請簽） |
| 簽名流水線 | 像水位線／簽核圖；點人可換代理人、換簽核、退回、發通知；點標題看個人資料 |
| 對話室 | 像 LINE：一人打一行→一格；回覆再一格；格子可**限定只能填某些欄** |
| 欄位分層 | **需簽核可見** vs **僅自己可見**；依登入／手機對應 ID 渲染；隱私欄不在共用庫 |

### 4A.0b 架構地圖（12345 同一畫面，3＝主錨）

```
1 登入 ID（Entra／手機對應同一人）
    ↓
2 JSON 表單引擎（型態／條件／顯示／唯讀）＋紙本畫面＋設計預覽
    ↓
3 雙層資料家 ←── 主錨
      ├─ 共用：SharePoint 清單（要簽的欄、狀態、流水線）
      └─ 個人：個人空間（僅自己可見；別人畫面無此內容）
    ↓
4 簽名流水線（點人：換代理／換簽核／退回／通知／看資料）
    ↓
5 對話室（LINE 格）＋可選 Teams 通知
```

記憶口訣：冰箱（3）分兩格——共用格與上鎖個人格；上面燈（2）是紙本表單引擎；燈上鈕（1）是身分；旁箭頭（4）是簽名流水線；下（5）是對話格與通知。

### 4A.1 關卡儀表板（Approval）

| 關卡 | 內容 | 狀態 | 進度 |
|------|------|------|------|
| **A0** | 架構＋路徑＋產品願景對齊 | ✅ 完成 | 100% |
| **A1** | 定稿欄位 schema（型態／條件／可見／storage） | ▶ 進行中 | 40% |
| **A2** | Teams 風格假畫面＋GitHub Pages | ▶ 進行中 | 80% |
| **A3** | 簽名流水線點擊行為（換人／退回／通知／個人卡） | ⬜ 未開始 | 0% |
| **A4** | 對話室（LINE 格＋限定欄位） | ⬜ 未開始 | 0% |
| **A5** | 雙層儲存 PoC（共用清單＋個人欄依 ID） | ⬜ 未開始 | 0% |
| **A6** | 登入＋手機開啟＋可選 Teams 通知 | ⬜ 未開始 | 0% |

**Approval 總進度：約 28%（A0 完成；A1／A2 進行中）。**  
假畫面路徑：`docs/` → GitHub Pages（合併 main 後約為 `https://hyi1105.github.io/Approval/`）。

### 4A.2 重要澄清（常問）

| 問題 | 簡答 |
|------|------|
| 公司資料庫怎麼連？ | 多數人連不到內網 SQL。共用欄用 SharePoint；隱私欄用個人空間＋ID。 |
| 為什麼隱私欄不放同一個清單？ | 放同一個清單別人仍可能被權限／匯出看到。你的規則是「別人那邊不存在」→ **個人儲存＋畫面依 ID 組裝**。 |
| Power Automate？ | 加價功能不依賴；通知可之後用 Graph 打 Teams。 |
| PnP／Python？ | 寫入共用清單可用；前端表單引擎先用 JSON＋網頁。 |
| 網頁誰管？ | 本倉庫 AI／Agent 維護紙本風網站與 schema。 |
| Agent 能看機敏明文嗎？ | **預設不行。** 見 §4A.2b：元素同構假資料（數字→數字、文字→文字、符號原樣），需解鎖才見明文。 |

### 4A.2b 機敏資料對 Agent（已定：元素同構遮罩）

目標：**AI 可全權維運程式／結構／系統**；機敏內容進到 Agent／匯出測試時預設是「鎖住的假值」。  
硬需求（使用者 2026-08-05）：假資料必須**元素同構**——數字仍是數字、文字仍是文字、符號仍是符號；**幾個位元組就幾個位元組**（匯出有定長／位元組偏移取值）。

| 項目 | 約定 |
|------|------|
| 誰管程式與維運 | Agent 可改程式、schema、部署、看**結構與遮罩後資料** |
| 誰看明文 | 僅經「解鎖」後、限時；解鎖核准人待定（A 本機／B 雙人／C 時段） |
| 閘道 | Agent **不直連**含機敏欄的正式庫；只經資料閘道／MCP，預設回遮罩值 |
| **同構規則（已定）** | 逐字元分類替換：**數字→數字、字母→字母、中日韓→中日韓、符號／空白→原樣不動** |
| **長度規則（已定）** | **字元數不變**，且在指定**匯出編碼**（如 Big5／UTF-8）下 **位元組數也不變**（定長切片才不會跑位） |
| 半形數字 `0–9` | 一律改 `9`（`1280000`→`9999999`） |
| 全形數字 `０–９` | 一律改 `９`（保持全形＝通常仍 2 或 3 bytes，視編碼） |
| 半形英文字母 | 改 `X`／`x`（對應原本大小寫） |
| 中文等寬文字 | 改同編碼寬度的假字（預設 `〇`；若該編碼下位元組數不合，改用同寬替代字） |
| 符號、`-`、`/`、`.`、空白、換行 | **逐字保留原樣**（`0912-345-678`→`9999-999-999`） |
| 匯出／定長檔 | 遮罩後字串必須通過「與原文同編碼下 `byteLength` 相等」檢核，否則換替代字 |
| schema 標記 | `sensitivity: open \| locked`；可選 `export_encoding`（預設依專案匯出設定） |
| 與雙層儲存 | `storage: personal` 與 `sensitivity: locked` 常一起用；共用格也可標 `locked`（如業績） |
| Cursor | Privacy Mode；憑證不進 repo；`.cursorignore` 擋 dump；遮罩仍可能外送推論——正式庫勿整表給 Agent |

#### 4A.2c Cursor 本機威脅（人類可控之外）

遮罩／閘道／解鎖＝**你能控制的那層**。下面是 **Agent／Cursor 本身** 還能碰到什麼（依官方文件，非保證零風險）。

| 問題 | 實話 |
|------|------|
| Agent 會不會「繞過」你的遮罩去拿明文？ | **若明文仍在它碰得到的地方，有機會。** 遮罩只保證「經閘道給它的預設是假資料」。若本機有明文 dump、它經 Terminal／`cat`／MCP 讀到、或你核准了危險指令，就可能繞過預期保護。 |
| `.cursorignore` 夠不夠？ | **不夠當硬牆。** 官方：擋 Agent 讀檔／索引／@；**擋不住 Terminal 與 MCP**；且「不是 security boundary」。 |
| Privacy Mode？ | **防訓練，不防「這次推論把 context 送出」。** 進對話的內容仍可能到模型供應商。 |
| 桌面／其他敏感檔會被偷走嗎？ | **不會無故自動掃整台電腦。** 但 Local Agent 與你的登入帳號**沒有隔離邊界**（官方：no security boundary between agents and your user account）。預設靠 sandbox＋外檔保護＋你核准；若開 **Run Everything**、把 Desktop 當專案打開、或核准出 sandbox 的指令，就可能讀到桌面／他處你帳號能讀的檔。 |
| Cloud Agent？ | **碰不到公司 PC 桌面**；只碰雲端 clone 的授權 repo（另一套風險：雲端暫存＋自動跑指令＋網路）。 |
| 「藏起來的帳密」安全嗎？ | **不安全（只要你自己還看得到）。** Agent 以**你的身分**執行 → 凡你帳號能開啟的檔／瀏覽器已存密碼／記事本／Excel／`.env`／USB 路徑，原則上 Agent 在核准或 Run Everything 後也能讀。**藏路徑、改檔名、`.cursorignore` 都不算對你帳號的隔離。** |

**身分＝鑰匙（已定）：**

> 你能用自己的身分看到的東西，就不能假設 Cursor Agent「絕對看不到」。  
> 真隔離只有：**這台／這個帳號根本讀不到**（另一個受限帳號、公司 Vault 短效權杖、OS ACL 拒絕你的 Cursor 帳號、或機敏根本不落地）。

| 做法 | 有沒有用 |
|------|----------|
| 密碼寫在怪資料夾／改怪檔名 | ❌ 你找得到＝Agent 也可能找到 |
| `.cursorignore` | ❌ 輔助而已；Terminal 可繞 |
| 記事本／Excel／瀏覽器記住密碼 | ❌ 同帳號可被讀或被工具叫出 |
| 每次拒絕可疑讀檔／指令 | ✅ 降低，但靠你盯 |
| 禁止 Run Everything | ✅ 必要門檻 |
| 密碼改放公司 Vault；本機只留短效 token | ✅ 較實在 |
| 另開「開發用」Windows／AD 帳號跑 Cursor，機敏目錄 ACL 不給該帳號 | ✅ 真隔離 |
| 正式庫帳密根本不進這台 PC | ✅ 最乾淨 |

**公司機硬規則（已定，給自己與 Agent 遵守）：**

1. Agent 執行模式用 **需核准／Auto-review**，**禁止 Run Everything**。  
2. **不要**用 Cursor 打開整個 Desktop／家目錄／含機敏匯出的資料夾當 workspace。  
3. 機敏明文、正式庫連線、dump：**不要放在開著的專案目錄**；專案內用 `.cursorignore` 仍只當輔助。  
4. 給 Agent 的資料通路只走**遮罩閘道**；不解鎖就沒有明文。  
5. 開 **Privacy Mode**（團隊能強制更好）；不要把 Cursor 當「資料不出本機」。  
6. 桌面／Excel／客戶名單等：靠 **OS 權限與自己不放進 workspace**；不要假設 Cursor 會自動避開。  
7. **帳密不要「藏在本機給自己用」當防護**；改 Vault／短效／另一受限帳號，或接受「這台能看＝Agent  theoretically 也能看」。
### 4A.3 A1 本關（欄位 schema）

- 過關條件：使用者確認範例 `schema/form-schema.example.json` 的欄位型態、`storage: shared|personal`、以及 `sensitivity`／Agent 遮罩規則 OK，或指出要改的 1～3 點。
- **本關剩餘決策（1 題）：** 解鎖核准＝A 本機／B 雙人／C 時段。
- **安全關（並行）：** §4A.2c 已定公司機硬規則；使用者知悉「本機 Agent ≠ 與 OS 隔離」即算對齊。
---

## 4B. 暫停進度（動手學大模型／dive-into-llms）

- 專案：`https://github.com/Lordog/dive-into-llms`
- **學習偏好：關卡制＋完成百分比**；提問可當關卡內工具，但**不可無限追問**。
- **暫停點（2026-07-30）：使用者選 A 收工。** 總進度 67%（G0–G3 完成）。下次從 **G4 Ch1 檔案導讀** 開始。
- 簽核主題已改由 **§4A** 承接，不再標「晚點實作」。

### 4B.0 進度儀表板（dive-into-llms，暫停）

| 關卡 | 內容 | 狀態 | 進度 |
|------|------|------|------|
| **G0** | 工具與方法（GitHub／NotebookLM／約定檔） | ✅ 完成 | 100% |
| **G1** | 全盤 11 章架構＋關鍵詞圖像記憶 | ✅ 完成 | 100% |
| **G2** | 每章「做什麼／結果」內容導覽 | ✅ 完成 | 100% |
| **G3** | Ch1 概念決策（微調／部署／標籤／推理／Skill／匯入類比） | ✅ 完成 | 100% |
| **G4** | Ch1 檔案導讀（README 流程 ↔ notebook 段落對應） | ⬜ 未完成 | 0% |
| **G5** | 選定下一深挖章（Ch2 或 Ch9…）並開新關卡 | ⬜ 未完成 | 0% |

**目前總進度（G0–G5）：約 67%（4／6 關完成）。**  
剛結束的會議目標「三小時搞懂去開會」≈ **G0–G2 已達成**；G3 為加分收束。

### 4B.1 全盤架構（11 章地圖）

```
動手學大模型
├── A 會用／會改模型
│   ├── Ch1 微調與部署
│   ├── Ch2 提示與思維鏈（不一定改權重）
│   ├── Ch3 知識編輯
│   ├── Ch4 數學推理（迷你 R1／蒸餾）
│   └── Ch11 RLHF 對齊
├── B 安全與攻防／隱匿
│   ├── Ch5 水印
│   ├── Ch6 越獄
│   ├── Ch7 隱寫
│   └── Ch10 智能體安全
└── C 多模態與 Agent
    ├── Ch8 多模態
    └── Ch9 GUI Agent
```

每章共通形狀：`README` 導讀 + `.pdf` 講義 + `.ipynb` 實驗。

### 4B.2 三小時最大化學習／演示行程（全部 11 章）

目標：三小時內 **每一章都學到「問題→做法→產出→1 個關鍵詞」**；先架構再逐章填空。  
深度上限：不跑完整訓練／不部署；以 README＋NotebookLM＋我整理為主。

時間預算：180 分 ≈ 架構 25 分 ＋ 11 章×12 分（132）＋ 收斂演示 23 分。

| 時段 | 分鐘 | 做什麼 | 產出 |
|------|------|--------|------|
| Block 0 | 0–10 | 對齊：成功＝11 章卡片齊；方法＝架構→逐章填空 | 電梯簡報 |
| Block 1 | 10–25 | 全庫架構（A/B/C）＋每章一句「在解決什麼」 | 架構圖 |
| Block 2a | 25–61 | **Ch1–Ch4** 各 9 分：讀／問 → 填卡片 | 4 張卡 |
| Block 2b | 61–97 | **Ch5–Ch8** 各 9 分 | 4 張卡 |
| Block 2c | 97–133 | **Ch9–Ch11** 各 12 分（略深因較新） | 3 張卡 |
| Block 3 | 133–160 | 串成故事線：從「會用模型」到「安全／Agent」 | 7 分鐘講稿 |
| Block 4 | 160–180 | 自測 11 題（一章一題）＋標 3 個下次深挖章 | 會／模糊／不會 |

每章固定卡片格式（強制，逾時就填「待補」往下一章）：
1. 問題（一句）
2. 做法（一句）
3. 產出／實驗檔（README／pdf／ipynb）
4. 關鍵詞（1 個）

NotebookLM 策略：根 README 必加；逐章以該章 `README.md` 為主（不夠再加 pdf）。每章提示詞固定：
「只根據來源，用台灣繁體、媽媽說故事語氣，填：問題／做法／產出／1 關鍵詞。禁止實作細節與參數。」

刻意不做：完整微調、Gradio 上線、公司簽核、單章鑽超過時限。

**加速策略：** Agent 可先給 11 章完整卡片草稿，使用者用 NotebookLM 核對／訂正，把時間花在記憶與串講，而非從零搜尋。

進度有變時，請更新本節。

## 5. 英語學習（配合使用者個性）

- 使用者傾向：**有真實需要才認真用**；啟動成本高就容易放棄。
- 主方法：真實郵件／回覆 → AI 協助潤成英文 → **手打第二遍**內化。
- 優化小步驟：先自己寫一版 → 請 AI 列出可套用句型與不自然處 → 每封信只留 1 句進累積本。
- 整理英語素材時：可幫使用者做成 Markdown／Google 文件結構，方便進 NotebookLM。
- **Google 文件（Docs）** 當 NotebookLM 來源較可能自動同步；GitHub 網址／上傳靜態檔通常不會自動更新。
- 目前無法在未接 Google API／自動化前，由 Agent 直接寫入使用者的 Drive；以約定檔＋整理稿降低手貼負擔。

---

## 6. 記憶方法偏好

- 優勢：對數字敏感、路線／空間關係強、筆記像心智圖、圖像聯想。
- **使用者指定的主系統（必須遵守）：**
  1. 以 **12345 為同一個畫面**（一次約五個），不是彼此無關的分立站點。
  2. **以 3 為主錨**，先聯想相鄰的 **2 與 4**；再 **1↔2**、**4↔5**。
  3. 空間位置要對應數字關係（例如 2 在 3「上面／最接近」、1 在 2 上、4 當方向／箭頭承載 3、5 在 4 下方等），以便「問幾號就能從畫面定位」。
  4. 11 個關鍵詞拆成：**畫面一 Ch1–5**、**畫面二 Ch6–10**（8 當該組的「3」主錨）、**Ch11 接在畫面二**。
- 長單字才用字母拆圖；工具保持低摩擦。
- 範例（Ch1–5，使用者認可）：3 編輯＝冰箱改價 → 上為 2 提示燈 → 燈上 1 微調鈕 → 冰箱站在 4 推理方向線 → 線下 5 浮水印水灘。

---

## 7. 回覆風格

- 直接、精簡；長答先給結論。
- 少用整句粗體刷屏。
- 技術概念優先用使用者已聽過的比喻延續（訓練／label／Skill vs 訓練等）。
- 使用者說「都可以／沒想法」時，由 Agent **選一條路徑並說明順序**，不要丟回二選一僵住。
- **教學模式（現行，重要）：關卡制＋百分比；提問服務於過關，不無限延伸。**
  - 每次回覆盡量標明：**目前關卡、本關 %、過關條件**。
  - 一關內最多約 **3 道決策題**；題目結束必須給「本關完成／未完成」結算，不得無止境追問。
  - 仍可用短題維持優化感，但要接已知情境；認知跳躍太大時立刻降級出題。
  - 興趣或成就感下降時：先結算進度，再開下一關，不要繼續問。

---

## 8. 如何更新本約定

1. 在對話中告訴 Agent 新規則。
2. Agent 應**改本檔（與 `.cursor/rules` 對應內容）並提交／推上 GitHub**。
3. 使用者以 GitHub 上的本檔為最終依據。

---

## 9. 快速檢查清單（Agent 每次回覆前）

- [ ] 繁體中文、台灣用語  
- [ ] 若在教操作：是否只給下一步  
- [ ] 若給 NotebookLM 語音提示詞：是否含「台灣口語／媽媽說故事」  
- [ ] 是否先讀過本檔最新版  
