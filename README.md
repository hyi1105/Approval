# Approval / 環島工具集

## 網頁路線工具（推薦）

[`trip-tool/`](./trip-tool/) — 在台灣地圖上依地址標註停留點、規劃路線、顯示停留／騎乘時間、上傳照片。

```bash
cd trip-tool && python3 -m http.server 8080
# 開啟 http://localhost:8080
```

## 3×2 拼貼產生器

[`trip-collage/`](./trip-collage/) — 用 Pillow 把六張照片拼成白底行程標籤拼貼圖。
