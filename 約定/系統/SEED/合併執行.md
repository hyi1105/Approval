# 合併執行（Approval／AI_MD → SEED）

最後更新：2026-08-13  
給：**有 SEED 推送權的 Agent**。地圖見 [`repo-atlas.md`](./repo-atlas.md)。

## 前置

- 工作區開 `hyi1105/SEED`（不要只開 Approval）
- 分支名：`cursor/repo-consolidate-from-approval-cc8c`（或同等）
- 本輪 Approval 側已備：模組化 `約定/`＋本檔＋`交給SEED/可貼開場.md`

## 步驟（照做）

### 1. 遷入約定（Approval → SEED）

```bash
# 在 SEED 倉庫根
git clone --depth 1 https://github.com/hyi1105/Approval.git /tmp/Approval-src
rsync -a --delete /tmp/Approval-src/約定/ ./約定/
# 根目錄轉址（可選）
cp /tmp/Approval-src/學習約定.md ./學習約定.md
mkdir -p .cursor/rules
cp /tmp/Approval-src/.cursor/rules/user-learning-agreements.mdc .cursor/rules/
# system-map skill
mkdir -p .cursor/skills
rsync -a /tmp/Approval-src/.cursor/skills/system-map/ .cursor/skills/system-map/
```

刪或縮短 SEED 內過時的 `approval/學習約定.md` 巨檔：頂部改「請改讀 `/約定/README.md`」。

### 2. 遷入 AI Doc（AI_MD → SEED）

```bash
git clone --depth 1 https://github.com/hyi1105/AI_MD.git /tmp/AI_MD-src
mkdir -p tools/ai-md
rsync -a /tmp/AI_MD-src/aidoc/ ./tools/ai-md/aidoc/
rsync -a /tmp/AI_MD-src/docs/ ./tools/ai-md/docs/
rsync -a /tmp/AI_MD-src/prompts/ ./tools/ai-md/prompts/
# 不要複製 web/ 底下建置產物（體積大、可 npm run build 再生）
```

在 `tools/ai-md/README.md` 寫：規格以 `約定/系統/SEED/AI-Doc.md` 為準；本目錄是可執行源碼。

### 3. 更新 SEED README

頂部加「總部地圖」表（同 repo-atlas 四列）。  
公司簽核段落維持；加 AI Doc：`tools/ai-md/aidoc/`、本機 `npm install && npm run build`。

### 4. 更新 memory

- 改寫 `memory/topics/repo-org-and-token.md`：狀態改「已決定真遷入」；連到 SEED `約定/系統/SEED/repo-atlas.md`
- `memory/index.md` 加一筆「Repo 總圖」
- （可選）`docs/seeds.json` 加 seed：`ai-doc`、`repo-atlas`（path 指到對應 md／工具頁）

### 5. 舊庫轉址（有權再做）

| Repo | README 頂部加 |
|------|----------------|
| Approval | 「可執行與約定已遷 SEED；本庫僅封存／轉址」＋連結 |
| AI_MD | 「已遷 SEED `tools/ai-md/`＋`約定/系統/SEED/`」＋連結 |

驗收通過後再 **Archive**（勿先刪）。

### 6. 不要做

- 不要把 `down-the-stairs` 程式拷進 SEED  
- 不要整包塞 AI_MD `web/` 建置檔  
- 不要再建 STATUS／HOW 巨檔  

## 驗收

- [ ] SEED 根有 `約定/核心.md`，Agent 規則指向它  
- [ ] `tools/ai-md/aidoc/` 可 `npm install && npm run build`  
- [ ] README 一眼看到 SEED／Approval／AI_MD／遊戲  
- [ ] Pages 既有 approval／walkthrough 仍可用  
- [ ] Approval／AI_MD README 已轉址（或 PR 待合）  

## 本輪（Approval 庫）已完成

- idea 已記  
- repo-atlas＋本執行檔  
- 主線改以 SEED 為總部  
- `交給SEED/可貼開場.md` 可直接貼給 SEED Agent  
