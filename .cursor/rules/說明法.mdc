---
description: 說明法——讀文件／讀程式／血緣／流程圖／跑指令前（可複製到任何 repo）
alwaysApply: true
---

# 說明法（永遠）

口令：「用說明法」或「結構＋介紹」。

適用：repo 結構、文件、原始碼、解法、欄位血緣、流程圖（非線性可先用）、請跑指令前介紹。

## 必守

1. 真實結構樹（path／標題／函式／if／for／欄位名／檔案名；跑腳本＝腳本路徑＋步驟）。
2. 父層同行介紹＋子層同行旁註；語氣「都在這裡面／放這…」；不要職稱標籤腔。
3. 一次最多 5 條；下一批空一行直接接；不要重複父層標題；不必等「繼續」。
4. 讀原始碼：左邊盡量貼近原文（少用 `...`）；一層一層——先外層，再進 if／for 內。
5. 欄位血緣：先「從哪來」，再跟一條欄位從頭走到尾；條件血緣與名字不一致要標出。
6. 流程圖／跨系統：先系統欄 → 再時間／步驟圈 → 再跟一份檔走完。
7. 請跑 `.ps1`／指令前：先說明「會動到什麼／不會動什麼／跑完會長怎樣」；等對方同意再跑。
8. 禁止：房間／故事比喻、虛流程、一次 >5、沒介紹就叫 Run。

## 範本

### 資料夾

```text
web/ 客人在網上看的東西都在這裡面
├── index.html     — 一進來先看到的那頁放這
├── query.html     — 要查工具時開的頁面放這
├── catalog.json   — 工具名單資料都寫在這一份
├── approval/      — 簽核相關的示範畫面都放這
└── system-map/    — 系統地圖相關畫面都放這

├── tool.html      — 看單一工具詳情的頁面放這
├── tool.js        — 上面那頁會用到的程式放這
├── script.js      — 查詢頁會用到的程式放這
├── shared.js      — 好多頁共用的程式放這
└── style.css      — 網站看起來長怎樣的樣子放這
```

### 程式（一層一層）

```text
function doSubmit() { 真的送出申請
├── const gaps = requiredGaps();
│     — 先問還缺什麼
├── if (gaps.length) { ... return; }
│     — 有缺就不往下送
├── markMissing([]);
│     — 沒缺就清紅字
├── status = "submitted";
│     — 狀態改成已送出
└── appendSystem({...}); showToast(...); refreshPrimaryAction();
      — 留訊息、提示、更新大按鈕
```

### 跑腳本前

```text
scripts/start.ps1 本機把服務開起來用（你同意後再跑）
├── Set-Location …
│     — 先切到專案資料夾
├── 啟動本機伺服器
│     — 用瀏覽器看現在的頁面
└── 不會部署上線、不會改遠端資料
      — 只是本機預覽
```

### 欄位血緣

```text
leave_type 假別（跟一條欄位從頭走到尾）
├── schema: "name": "leave_type"
│     — 規格裡叫這個名字
├── index.html: name="leave_type"
│     — 畫面上的下拉選單就是它
├── requiredGaps() 會掃 data-required
│     — 空的就不能送出
├── doSubmit() → status = "submitted"
│     — 送出後這張單進已送出
└── conversation 可綁 leave_type
      — 對話預設會帶到這個欄位
```

### 跨系統流程（可先用）

```text
某流程 誰跟誰傳檔
├── 系統 A
│     — 左邊／來源系統
├── 中繼機
│     — 中間暫放、轉送
└── 系統 B
      — 右邊／目的平台

紅圈 1（時間／觸發）
├── 來源表／程式
│     — 資料從這出來
├── 產出檔 X
│     — 這一輪送出去的檔
└── → 下一站資料夾／系統
      — 檔先到這裡
```

## 安裝到別的 repo

1. 建立資料夾 `.cursor/rules/`
2. 新增檔案 `說明法.mdc`（或 `explanation-method.mdc`）
3. 貼上本檔全文（含上方 `---` frontmatter）
4. 開新對話即會套用（`alwaysApply: true`）

也可把下方口令卡貼進 Cursor → Settings → Rules → User Rules（所有專案）：

```text
用說明法：真實結構樹＋同行簡短介紹；一次≤5；空一行接續；
左邊盡量原始碼／原文；一層一層（先外層再進 if／for）；
欄位血緣：先從哪來，再跟一條欄位走完；
流程圖：先系統欄→步驟圈→跟一份檔走完；
不要房間比喻、不要職稱標籤腔、不必等我說繼續。
請我跑 .ps1／指令前，先用說明法講清楚，等我理解同意再跑。
```
